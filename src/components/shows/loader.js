import React from "react";
import { Dimmer, Loader, Segment } from "semantic-ui-react";

const Load = () => (
  <div>
    <Loader active size="huge">
      Searching For Shows
    </Loader>
  </div>
);

export default Load;
