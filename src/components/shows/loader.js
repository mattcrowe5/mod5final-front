import React from "react";
import { Loader } from "semantic-ui-react";

const Load = () => (
  <div>
    <Loader active size="huge">
      Searching For Shows
    </Loader>
  </div>
);

export default Load;
